================================================================
  PERSONAL SITE — VERSION HISTORY & DEPLOYMENT GUIDE
  آخرین نسخه: V14.0 — 2026-09-18
================================================================

## 📌 نسخه فعلی: V14.0

================================================================
  تاریخچه نسخه‌ها
================================================================

V14.0 (2026-09-18) — امنیت + دسترسی بخش‌بندی + آموزش آپدیت
  - حذف fallback admin123 (فقط دیتابیس معتبره)
  - SESSION_SECRET اجباری در production
  - Sanitization کد امبد آپارات (XSS prevention)
  - Rate limit برای login (۵ تلاش/۱۵ دقیقه)
  - Webhook secret اجباری
  - حذف plain-text password از SiteSetting
  - دسترسی بخش‌بندی کاربران (permissions)
  - فیلد رمز با دکمه نمایش/مخفی
  - تب پیام‌ها در پنل ادمین
  - HelpBox برای هر تب
  - آموزش آپدیت (V14)

V13.0 (2026-09-17) — کلیپ آپارات + ریست رمز + SEO
  - مدل AparatClip در دیتابیس
  - /api/admin/clips (CRUD)
  - /api/clips (عمومی)
  - /clips صفحه
  - AparatClipManager کامپوننت
  - reset-admin-password.sh
  - SEO کامل (OpenGraph, Twitter, JSON-LD)

V12.0 (2026-09-16) — دسترسی بخش‌بندی + دکمه رمز
  - فیلد permissions در AccessUser
  - فیلتر تب‌ها بر اساس permissions
  - دکمه 👁 برای نمایش/مخفی رمز

V11.0 (2026-09-16) — پیام‌ها + HelpBox
  - تب پیام‌ها در داشبورد
  - HelpBox برای هر تب
  - re-enable همه کامپوننت‌ها

V10.0 (2026-09-15) — نصب مستقیم (بدون Docker)
  - install.sh با Node.js + Nginx
  - Swap خودکار
  - npm با mirror چین

V9.0 (2026-09-15) — غیرفعال‌سازی + کلیپ
  - (بعداً re-enable شد)

V8.0 (2026-09-16) — SEO + clean seed
  - metadataBase, openGraph, twitter
  - robots.txt کامل
  - manifest.json کامل

V7.0 (2026-09-16) — پنل ادمین جدید
  - /user-dashboard به‌عنوان پنل اصلی
  - FontSelector (۵ فونت)
  - beforeunload protection
  - Ollama guide

V6.0 (2026-09-16) — رفع admin login
  - checkAdminPassword از دیتابیس
  - همه APIها از admin-auth استفاده می‌کنن

V5.0 (2026-09-16) — رفع متن‌ها
  - useContent از /api/content
  - ۳ زبان (fa/en/de)
  - حذف cache

V4.0 (2026-09-16) — رف path دیتابیس
  - db.ts با process.cwd()
  - .env با مسیر مطلق

V3.0 (2026-09-16) — نصب خودکار
  - install.sh
  - Swap
  - mirror چین

V2.0 (2026-09-15) — رفع خطاهای نصب
  - package.json dev script
  - docker-entrypoint.sh
  - seed scripts با مسیر نسبی

V1.0 (2026-08-10) — ساخت اولیه
  - Next.js 16 + TypeScript + Prisma + SQLite
  - تم ترمینال
  - پنل ادمین
  - ۳ زبان

================================================================
  نصب
================================================================

wget https://github.com/ldrcoir/ehsan-site/raw/main/public/install.zip
unzip install.zip
cd personal-site
sudo ./install.sh

ورود ادمین:
  http://YOUR_IP:3000/user-login
  username: admin
  password: admin123
  ⚠️ از پنل رمز رو عوض کن!

================================================================
  ریست رمز ادمین (اگه فراموش کردی)
================================================================

cd /home/ehsan/personal-site
sudo bash scripts/reset-admin-password.sh

================================================================
  آموزش
================================================================

فایل TUTORIAL_FA_V14.docx رو بخون — ۸ فصل کامل:
  ۱. مقدمه
  ۲. نصب
  ۳. امنیت
  ۴. سیستم کاربران
  ۵. کلیپ آپارات
  ۶. هوش مصنوعی (Ollama)
  ۷. SEO
  ۸. جمع‌بندی

همچنین OLLAMA_GUIDE_FA.md برای آموزش هوش مصنوعی محلی.

================================================================
