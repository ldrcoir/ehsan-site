================================================================
  PERSONAL SITE — VERSION HISTORY & DEPLOYMENT GUIDE
  آخرین نسخه: V17.3 — ۲۰۲۶-۰۹-۲۶
================================================================

## 📌 نسخه فعلی: V17.3

================================================================
  نصب سریع (یک دستور)
================================================================

wget https://github.com/ldrcoir/ehsan-site-private/raw/main/public/install-v17.3.zip
unzip install-v17.3.zip
cd personal-site
sudo ./install.sh

ورود ادمین:
  http://YOUR_IP:3000/user-login
  username: admin
  password: admin123
  ⚠️ حتماً از پنل → تنظیمات → تغییر رمز عوض کن!

================================================================
  آمار ممیزی V17.3
================================================================

✅ ۲۰ worker parallel audit اجرا شد (AUDIT-1 تا AUDIT-20)
✅ ۱۹ worker report داد — ۱ fail
✅ همه CRITICAL و HIGH blockers رفع شد

================================================================
  تاریخچه نسخه‌ها
================================================================

V17.3 (2026-09-25) — ۲۰ ممیزی parallel + رفع بحران‌ها
  
  رفع بحران‌های V17.1:
  ✅ Honeypot + time-trap در فرم تماس (قبلاً dead code بود)
  ✅ CSP frame-src شامل google.com (reCAPTCHA کار می‌کنه)
  ✅ install.sh: HTTPS با certbot + RECAPTCHA placeholder
  ✅ .env از git tracking حذف شد (.secret دیگر در repo نیست)
  ✅ db/custom.db از standalone package حذف شد
  ✅ change_password به currentPassword نیاز داره
  ✅ /api/messages از PUBLIC_API_PREFIXES حذف شد (CSRF protection فعال)
  ✅ Body size limit 1MB در middleware
  ✅ HSTS header روی HTTPS
  ✅ /api/admin/providers update فیلدهای name + enabled رو قبول می‌کنه
  ✅ SettingsPanel: name + tagline از API لود می‌شه
  ✅ SettingsPanel: تگ adminTagline/adminStatus inputs اضافه شد
  ✅ global-error.tsx + error.tsx + not-found.tsx + loading.tsx اضافه شد
  ✅ Cookie secure بر اساس NODE_ENV (نه همیشه true)
  ✅ SESSION_SECRET lazy-loaded (build بدون env کار می‌کنه)
  ✅ outputFileTracingIncludes بدون db/custom.db
  
  ویژگی‌های جدید:
  ✅ Tagline editable در ۳ زبان (fa/en/de)
  ✅ تگ adminTagline و adminStatus در SettingsPanel
  ✅ آموزش کامل V17.3 (TUTORIAL_FA_V17.3.docx — ۴۷KB)
  ✅ README.md در پکیج

V17.1 (2026-09-25) — بازنویسی کامل پنل ادمین
  ✅ middleware.ts با CSRF + rate limit + CSP
  ✅ SettingsPanel بازنویسی کامل
  ✅ ContentManager shape mismatch با API رفع
  ✅ حذف ?password= از همه fetch ها
  ✅ Language switcher واقعی (fa/en/de + RTL)
  ✅ AI Provider config UI
  ✅ Telegram bot config UI
  ✅ Beautiful CSS classes
  ✅ Accessibility (aria-labels, focus-visible)
  ✅ Responsive design

V16.0 (2026-09-24) — ممیزی امنیتی اولیه
V15.0 (2026-09-19) — حذف admin123 fallback
V14.0 (2026-09-18) — امنیت + دسترسی بخش‌بندی
V13.0 (2026-09-17) — کلیپ آپارات + ریست رمز + SEO
V12.0 (2026-09-16) — دسترسی بخش‌بندی + دکمه رمز
V11.0 (2026-09-16) — پیام‌ها + HelpBox
V10.0 (2026-09-15) — نصب مستقیم
V9.0 (2026-09-15) — کلیپ آپارات
V8.0 (2026-09-16) — SEO + clean seed
V7.0 (2026-09-16) — پنل ادمین جدید + FontSelector
V6.0 (2026-09-16) — رفع admin login از دیتابیس
V5.0 (2026-09-16) — رفع متن‌ها (۳ زبان)
V4.0 (2026-09-16) — رفع path دیتابیس
V3.0 (2026-09-16) — نصب خودکار + Swap + mirror
V2.0 (2026-09-15) — رفع خطاهای نصب
V1.0 (2026-08-10) — ساخت اولیه

================================================================
  نصب
================================================================

wget https://github.com/ldrcoir/ehsan-site-private/raw/main/public/install-v17.3.zip
unzip install-v17.3.zip
cd personal-site
sudo ./install.sh

ورود ادمین:
  http://YOUR_IP:3000/user-login
  username: admin
  password: admin123
  ⚠️ حتماً از پنل → تنظیمات → تغییر رمز عوض کن!

⚠️ مهم — تنظیم reCAPTCHA بعد از نصب:
  1. https://www.google.com/recaptcha/admin برو
  2. سایت v2 ثبت کن
  3. Site Key و Secret رو در .env بذار
  4. sudo systemctl restart personal-site

================================================================
  ریست رمز ادمین (اگه فراموش کردی)
================================================================

cd /home/ehsan/personal-site
sudo bash scripts/reset-admin-password.sh

================================================================
  امنیت (V17.3)
================================================================

✅ Authentication Bypass — رفع شد
✅ Hardcoded admin123 — حذف از src/
✅ SESSION_SECRET — قوی + entropy check + lazy-load
✅ reCAPTCHA — اجباری + fail-closed + honeypot + time-trap
✅ timingSafeEqual — همه webhook و session
✅ XSS prevention — sanitize-embed + scheme validation
✅ Rate limit — login (5/15د)، contact/chat (8/15د)، body 1MB
✅ CSRF protection — Origin check در middleware + /api/messages هم شامل شد
✅ Webhook secrets — اجباری + HMAC-safe comparison
✅ Cookie secure:true (production) + sameSite:strict + httpOnly
✅ Security headers: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS
✅ middleware.ts: server-side admin protection
✅ Global error pages (error.tsx, global-error.tsx, not-found.tsx, loading.tsx)
✅ change_password به currentPassword نیاز داره
✅ .env از git tracking حذف شد
✅ db/custom.db از standalone حذف شد
✅ ignoreBuildErrors: false
✅ reactStrictMode: true
✅ poweredByHeader: false
✅ ۰ خطای TypeScript
✅ systemd با user غیر root (ehsansite)
✅ .env با chmod 600
✅ db با chmod 600

================================================================
  قابلیت‌ها (V17.3)
================================================================

📊 ۱۰ تب پنل ادمین:
  - داشبورد
  - پیام‌ها (reply/delete)
  - محتوا (book/article/tutorial/skill/equipment)
  - متن‌ها (۳ زبان)
  - منو (nav links)
  - تم‌ها (color themes)
  - کاربران (access control + permissions + hours/days)
  - کلیپ‌ها (Aparat)
  - فونت (۵ فونت)
  - تنظیمات کامل:
    - Password (با current password)
    - Handle
    - Name × ۳ زبان
    - Tagline × ۳ زبان
    - Email forwarding
    - Bale bot
    - Telegram bot
    - AI Providers
    - Font selector
    - Panel language selector

🧠 AI Chat:
  - ۴ provider (OpenAI, Anthropic, Ollama, Groq)
  - Ollama محلی پشتیبانی می‌شه
  - API kill switch
  - Fallback chain

📨 Notification:
  - Bale bot
  - Telegram bot
  - Email forwarding (formsubmit.co — رایگان)

🌐 ۳ زبان:
  - فارسی (RTL)
  - English (LTR)
  - Deutsch (LTR)

🔒 امنیت:
  - bcrypt password hashing
  - HMAC-SHA256 session tokens
  - timing-safe comparisons
  - rate limiting everywhere
  - honeypot + time-trap on contact form
  - CSP + security headers
  - middleware.ts server-side protection
  - Body size limit 1MB
  - HSTS on HTTPS

📚 آموزش:
  - TUTORIAL_FA_V17.3.docx — ۱۲ فصل کامل (فارسی)
  - VERSION.txt — تاریخچه نسخه‌ها
  - README.md — راهنمای سریع
  - OLLAMA_GUIDE_FA.md — راهنمای هوش مصنوعی محلی

================================================================
